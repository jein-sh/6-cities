export type AuthData = {
  email: string;
  password: string;
};

export type FavoriteData = {
  id: number;
  status: number;
};

export type CommentData = {
  id: number;
  comment: string;
  rating: number;
};
